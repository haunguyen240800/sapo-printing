# Triage Working Document

## Normalized Findings (38 total)

### From Blind Hunter (14 findings)

1. **Timestamp panic risk in save()** — `.unwrap()` on `SystemTime::now().duration_since(UNIX_EPOCH)`. Panic if system clock before 1970. Location: printer_repository.rs:37-40
2. **Hardcoded defaults overwrite user config** — Every `save()` resets paper_size, margins, color_mode to hardcoded values instead of persisting actual printer state. Location: printer_repository.rs:69-82
3. **PrinterType hardcoded as Local in find_all()** — Database has printer_type column but code hardcodes `PrinterType::Local`. Location: printer_repository.rs:110
4. **PrinterType hardcoded as Local in find_by_name()** — Same issue. Location: printer_repository.rs:149
5. **device_id discarded during reconstruction** — `device_id` queried but ignored, `Printer::new()` creates new ID breaking idempotency. Location: printer_repository.rs:109, 148
6. **Status field ignored during reconstruction** — Schema has status column but queries don't fetch it, all printers loaded as Offline. Location: queries in find_all/find_by_name
7. **created_at preservation unclear in UPSERT** — Code structure misleading, looks like created_at might be overwritten. Location: printer_repository.rs:65
8. **test_save_default_printer_unsets_others doesn't test spec** — Test verifies both are non-default, not that one is default and others unset. Location: printer_repository.rs:222-243
9. **Mutex poisoning converts to generic error** — Poisoned mutex wrapped as generic RepositoryError hiding critical failure. Location: printer_repository.rs:26-30, 94-98, 134-138
10. **No concurrent access tests** — Uses Arc<Mutex> but no tests for race conditions. Location: test module
11. **No NULL buffer_size_kb read test** — Saved as NULL but never queried/tested. Location: printer_repository.rs:80
12. **printer_name not unique but used as key** — find_by_name() queries by printer_name but schema's unique constraint is on device_id. Location: printer_repository.rs:141
13. **Git commit template unclosed quote** — `.claude/settings.local.json` line 27 has unclosed single quote in commit pattern
14. **std::error::Error trait not shown** — New error variant added but diff doesn't show trait impl

### From Edge Case Hunter (16 findings)

15. **System clock before epoch** — printer_repository.rs:37-40, unwrap panic, guard: `.unwrap_or_else(|_| Duration::from_secs(0))`
16. **Printer config values not persisted** — printer_repository.rs:66-84, hardcoded defaults, guard: extract from printer aggregate
17. **Printer status not persisted** — printer_repository.rs:66-84, Online status lost, guard: add status field
18. **Printer type not persisted** — printer_repository.rs:66-84, type not saved, guard: add printer_type field
19. **Network printer loaded as Local (find_all)** — printer_repository.rs:107-110, hardcoded Local, guard: query and parse printer_type
20. **Online printer loaded as Offline (find_all)** — printer_repository.rs:107-110, status not queried, guard: query status and conditionally connect()
21. **device_id discarded in reconstruction** — printer_repository.rs:108-109, new ID created, guard: use stored device_id
22. **Two printers same name edge case** — printer_repository.rs:141, returns first match not intended, guard: UNIQUE on printer_name or use device_id
23. **Network printer loaded as Local (find_by_name)** — printer_repository.rs:146-150, hardcoded Local
24. **Mutex poisoning hidden** — printer_repository.rs:26-30, converts to generic error, guard: `.unwrap_or_else(|poisoned| poisoned.into_inner())`
25. **No transaction for is_default update** — printer_repository.rs:43-88, race condition, guard: use transaction for multi-step update
26. **SQL-breaking names** — printer_repository.rs:66-84, long names hit SQLite limits, guard: validate length
27. **Error message info leak** — printer_repository.rs:100-104, sensitive data in errors, guard: sanitize error messages
28. **Printer::new() failure unchecked** — printer_repository.rs:125, domain invariant violation, guard: check Result
29. **Test setup migration failure** — printer_repository.rs:171-174, unwrap panic, guard: handle migration error
30. **Test connect() failure** — printer_repository.rs:209, unwrap panic, guard: assert or expect

### From Acceptance Auditor (8 findings)

31. **is_default logic not implemented (AC-2)** — save() hardcodes is_default=0, no unset-others logic. Evidence: `0, // ?15 - is_default (false for now)`
32. **PrinterId used instead of stable device_id (Dev Notes)** — `printer.id().to_string()` violates "DO NOT store PrinterId" instruction
33. **find_all() doesn't read status/type from DB (AC-3)** — Query only selects printer_name, device_id; hardcodes PrinterType::Local
34. **find_all() doesn't call connect() for Online printers (AC-3)** — Missing conditional connect() after reconstruction
35. **find_all() doesn't drain events (Dev Notes)** — Missing `printer.drain_events()` call
36. **find_by_name() doesn't read status/type (AC-3)** — Same as #33
37. **find_by_name() doesn't call connect() or drain events (AC-3)** — Same as #34, #35
38. **test_save_default_printer_unsets_others wrong assertion (AC-6)** — Tests both printers have is_default=0, not testing actual requirement

## Deduplication Pass

**Group A: Timestamp panic (1, 15)** → Merge to FINDING-1
**Group B: Hardcoded defaults (2, 16, 17, 18)** → Merge to FINDING-2
**Group C: PrinterType hardcoded find_all (3, 19, 33)** → Merge to FINDING-3
**Group D: PrinterType hardcoded find_by_name (4, 23, 36)** → Merge to FINDING-4
**Group E: device_id discarded (5, 21, 32)** → Merge to FINDING-5
**Group F: Status ignored find_all (6, 20, 33)** → Merge to FINDING-3 (same query)
**Group G: Status ignored find_by_name (6, 36)** → Merge to FINDING-4
**Group H: Missing connect() find_all (34, 20)** → Merge to FINDING-6
**Group I: Missing connect() find_by_name (37)** → Merge to FINDING-7
**Group J: Missing drain_events find_all (35)** → FINDING-8
**Group K: Missing drain_events find_by_name (37)** → FINDING-9
**Group L: is_default not implemented (8, 31, 38)** → Merge to FINDING-10
**Group M: Mutex poisoning (9, 24)** → Merge to FINDING-11
**Group N: created_at preservation (7)** → FINDING-12
**Group O: No concurrent tests (10)** → FINDING-13
**Group P: No NULL buffer test (11)** → FINDING-14
**Group Q: printer_name not unique (12, 22)** → Merge to FINDING-15
**Group R: No transaction for is_default (25)** → Related to FINDING-10
**Group S: SQL name length (26)** → FINDING-16
**Group T: Error message leak (27)** → FINDING-17
**Group U: Printer::new() unchecked (28)** → FINDING-18
**Group V: Test setup unwrap (29)** → FINDING-19
**Group W: Test connect unwrap (30)** → FINDING-20
**Group X: Git commit quote (13)** → FINDING-21 (defer)
**Group Y: Error trait impl (14)** → FINDING-22 (defer)

## Deduplicated List (22 findings)

FINDING-1: Timestamp panic risk (blind+edge)
FINDING-2: Hardcoded defaults overwrite config (blind+edge+auditor)
FINDING-3: find_all() doesn't query status/type, hardcodes Local (blind+edge+auditor)
FINDING-4: find_by_name() doesn't query status/type, hardcodes Local (blind+edge+auditor)
FINDING-5: device_id discarded, PrinterId used instead (blind+edge+auditor)
FINDING-6: find_all() missing connect() for Online printers (edge+auditor)
FINDING-7: find_by_name() missing connect() for Online printers (edge+auditor)
FINDING-8: find_all() missing drain_events() (auditor)
FINDING-9: find_by_name() missing drain_events() (auditor)
FINDING-10: is_default logic not implemented (blind+auditor+edge)
FINDING-11: Mutex poisoning hidden as generic error (blind+edge)
FINDING-12: created_at preservation unclear (blind)
FINDING-13: No concurrent access tests (blind)
FINDING-14: No NULL buffer_size_kb test (blind)
FINDING-15: printer_name not unique but used as key (blind+edge)
FINDING-16: SQL name length validation missing (edge)
FINDING-17: Error message potential info leak (edge)
FINDING-18: Printer::new() failure unchecked (edge)
FINDING-19: Test setup migration unwrap (edge)
FINDING-20: Test connect() unwrap (edge)
FINDING-21: Git commit template syntax error (blind)
FINDING-22: Error trait impl not shown in diff (blind)
